name: env-test
on:
    workflow_dispatch:
jobs:
    env-test:
        name: env-test
        runs-on: ubuntu-latest
        environment: dev
        steps:
            -   name: echo secrets
                run: |
                    echo ${{ secrets.PARAM_ABC }}